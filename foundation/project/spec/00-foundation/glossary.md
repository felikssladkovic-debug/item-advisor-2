# Glossary

- `user`: An authenticated person represented by an email and role.
- `manager`: A user role with access to the admin users page.
- `site page`: The authenticated landing page for regular system use.
- `admin users page`: A restricted page that displays the known users in the system.
- `session`: The server-side record that links a browser cookie to an authenticated user.
