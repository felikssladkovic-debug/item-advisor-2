# Deployment

## Local Development

- The system runs with Docker Compose.
- MongoDB runs as a container.
- Backend runs as a container on port `8000`.
- Frontend runs as a container on port `5173`.

## First Generation Goal

- The deployment target is local development only.
- Production hardening is out of scope for this first generation.
