# Test Scenarios

Backend:
- login success
- login failure
- `me` authenticated
- `me` unauthenticated
- admin users as manager
- admin users as non-manager

Frontend site:
- render unauthenticated state
- basic login flow with mocked API

Frontend admin:
- render users table with mocked API
- render forbidden message with mocked API

