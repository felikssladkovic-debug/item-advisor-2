# Admin UI

The admin UI is intentionally minimal.

Visible states:
- loading users
- users table
- clear message for forbidden or unauthenticated access

The table is read-only and contains:
- email
- role
- created timestamp

