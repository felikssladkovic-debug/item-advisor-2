# Glossary

- Foundation version: the first small but deployable vertical slice used to validate the development method.
- Site app: the Vue application served at `/`.
- Admin app: the Vue application served under `/admin/`.
- Authenticated user: the user identified by the current session cookie.
- Manager: the only role allowed to read the admin users list.
- Seed users: the initial manager and user accounts created automatically on startup.

