# method-binding

## Назначение
Связать проект с методикой: какие файлы считаются источниками истины и как поддерживается трассировка.

## Базовое правило
Каждая существенная реализация должна иметь опору в `ideas/`, `spec/` или явном решении в `decisions.md`.
